import React from "react";

const header = {
    fontWeight: "bold"
};

const Header = () => (
    <div style={header}>
        <p>
            SportBook
        </p>
    </div>
)

export default Header;