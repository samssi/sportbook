import React from "react";

const header = {
    marginBottom: "15px",
    fontWeight: "bold"
};

const Header = () => (
    <div style={header}>
        <p>
            SportBook
        </p>
    </div>
)

export default Header;